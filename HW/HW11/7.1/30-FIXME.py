from fraction import Fraction

class Polynomial:
	def __init__ (self):
		self.a = eval (input ("Enter numerical coefficient a: "))
		self.b = eval (input ("Enter numerical coefficient b: "))
		self.c = eval (input ("Enter numerical coefficient c: "))
